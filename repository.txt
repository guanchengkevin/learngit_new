// 先创建一个目录，切换到目录 
mkdir learngit; 
cd learngit;

//初始化为仓库 repository
git init; 

//添加文件
git add file1 file2 ...; 
//将文件提交到仓库
git commit -m "instructions"

//查看当前仓库的状态的变化(新增或者修改) 
git status; 

//查看当前仓库的日志
git log;

cat file_name 
//可以查看repository中文件
